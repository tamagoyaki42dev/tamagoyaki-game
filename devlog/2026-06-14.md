# Devlog 2026-06-14

## GitHub Actions を整備しました。push のたびに GDScript 構文チェックが走り、main push で itch.io に自動デプロイされます。[skip-sns]

追加したもの：
- `.github/workflows/ci.yml` — gdtoolkit による GDScript 構文チェック（push/PR 毎）
- `.github/workflows/deploy.yml` — Godot HTML5 エクスポート → butler で itch.io デプロイ（main push 毎）
- `export_presets.cfg` — Web エクスポートプリセット
- `.env.example` に `BSKY_HANDLE` / `BSKY_PASSWORD` を追記

GitHub Secrets に `ITCHIO_API_KEY` / `ITCHIO_GAME_URL` を登録すればデプロイが自動化される。

## 09:57 GitHub Actions を整備しました。push で構文チェック、main push で itch.io 自動デプロイ。[skip-sns]

## 10:01 butler インストールを Ayowel/butler-to-itch.io アクションに変更 [skip-sns]

## 10:03 butler を josephbmanley/butler-publish-itchio-action に変更 [skip-sns]

## 仕様準拠の残タスクを全て実装

`battle_manager.gd` / `battle_unit.gd` の更新：

- **攻撃補助・防御補助**: 中列ユニットが同縦マスの前列を自動支援。`support_used` フラグで1回制限、ローテーション時にリセット。間接攻撃には補助不加算。
- **素早さ同値タイブレーク**: 同側同速→マス番号順（col 0→3）、味方vs敵同速→ランダム（ターン開始時に乱数値を事前生成）。
- **敵の思考タイプ**: 8種全実装（きまぐれ/弱者/強者/中央/補助/瀕死/女性/男性狙い）。前列→中列→後列のフォールバック付き。マス2/3優先の重み付きランダム選択も実装。

## 10:34 攻撃補助・防御補助・思考タイプ・速度同値タイブレークを実装。戦闘システムが仕様書完全準拠に

## 仕様書の残タスクを全て実装（battle_spec.md 完全準拠）

`battle_unit.gd` / `battle_manager.gd` / `battle_ui.gd` / `battle_test.gd` を更新：

- **クリティカル**: crit_rate 判定（サムライ・幻術師10%、他5%）、1.5倍乗算
- **クリティカルチャージ**: 余剰回復 → charge_excess 蓄積、2×HP以上で倍率発動（1.5〜4倍）、被弾で消滅
- **石化（STONE）**: 祈祷師の攻撃で石化付与。行動不可・被ダメ1.5倍。再石化で解除、ローテでも解除。解除後即行動
- **HP吸収（ABSORB）**: 敵がダメージの50%を自己回復
- **攻撃補助力(列)**: 同縦マストリガーで前列攻撃が全敵への列攻撃に（現状1体なので効果同じ）
- **前列空き2倍ダメ**: 中列への被ダメ×2（前列空き）、後列は前+中が空きで×2
- **ターン上限100ターン**: 超えたら強制撤退
- **端数切り上げ**: 全ダメージ計算を ceili に統一
- **石化解除後の即行動**: already_acted リストで重複実行防止
- `unit_petrified` / `unit_stone_cleared` シグナル追加
- `character_data.gd`: `atk_bonus_is_row` フィールドと `from_job()` ファクトリ追加（17職業・個体差±20%）
- `character_job.gd`: `crit_rate()` / `attack_hits()` / `is_stone_attack()` スタティック関数追加

## 11:12 クリティカル・石化・HP吸収・チャージを実装。戦闘システムが仕様書完全準拠に

## バトル画面のUI整備

- 闘技場の背景画像を追加（`assets/bg_arena.jpg`）
- ユニットカードに枠線・角丸・半透明背景（`_panel` ヘルパー）
- ローテーションボタン（オレンジ）・ステイボタン（シアン）をスタイリング
- バトルログエリアにパネル背景を追加
- ターン表示エリアに半透明帯を追加

## 11:26 バトル画面に闘技場背景・スタイル付きボタン・カード枠線を追加。見た目が本物っぽくなった

## 編成画面を実装

起動→編成→戦闘→編成、のゲームループが完成した。

追加したもの：
- `scripts/game_state.gd` — GameState（class_name静的クラス）。パーティプール・編成Dict・敵タイプをシーン間で保持
- `scripts/ui/formation_ui.gd` — 編成画面UI（グリッド3×4・ベンチ・右パネル詳細・敵タイプセレクター）
- `scenes/formation.tscn` — 新メインシーン
- `project.godot` — メインシーンを formation.tscn に変更
- `battle_manager.gd` — formation Dictからのエントリ（row/col付きDict）に対応
- `battle_ui.gd` — GameStateからパーティ・敵データを取得、戦闘終了2.5秒後に編成画面へ戻る

操作：
- キャラクターをクリックして選択（黄枠表示）
- 別グリッドセルをクリックでスワップ、ベンチキャラクリックでグリッド↔ベンチ交換
- 右パネルに選択キャラのステータス全表示（職業・HP・ATK・SPD・補助値・クリティカル率）
- 敵タイプは◀▶で切り替え（デバッグ用）

## バトル画面のビジュアルを全面強化

`battle_ui.gd` / `assets/` を更新：

- **背景画像**: AI生成の近未来闘技場（`assets/bg_arena.jpg`）＋暗めオーバーレイ
- **ボタン**: `StyleBoxFlat` でローテーション（オレンジ）・ステイ（シアン）をスタイリング。ホバー・押下状態付き
- **ユニットカード**: 160×95 → 190×115px に大型化。味方（青枠）・敵（赤枠）の角丸パネルに変更
- **カード情報**: ATK・SPD を追加表示。右端48pxにキャラ画像スロット（現在は「?」プレースホルダー）
- **ターン表示**: フォント22→30・ゴールドカラー・中央帯パネルに配置
- **BATTLE LOGヘッダー**: ログエリア上部に追加
- **レイアウト調整**: `PLAYER_Y0` を 0.53→0.58 に下げてボタンとカードの重なりを解消
- **バグ修正**: `battle_manager.gd` の Variant型推論エラー（`max()` → 型明示）

**キャラチップ素材の準備**

ぴぽや倉庫のキャラチップ（無料・商用可）を入手：
- `assets/characters/pipo/` — 30種類×色違いで130+パターン
- スプライトシート仕様: 96×128px、1フレーム32×32px、後ろ姿は y=96（上向き行）

## 12:34 バトル画面を全面刷新。近未来闘技場の背景・スタイル付きボタン・カード情報充実で別物に

## 12:37 [skip-sns] GameStateをオートロードからclass_name静的クラスに変更。エディタ再起動なしで認識される

## 次のセッションでやること

**キャラ画像の実装**
- ぴぽやキャラチップをカードの「?」スロットに表示（AtlasTextureで32×32切り出し）
- 職業グループ別のランダム割り当て対応

**ゲームループ**
- 拠点画面の実装（遠征後に戻る場所）→ ゲームループの骨格を完成させる
- 世代継承システム（撤退時にキャラクター死亡 → 次世代へ継承）
- 編成画面で撤退時のキャラ死亡を反映（pool から削除 or 無効化）

**UI改善（後回し可）**
- 石化状態をカード上に視覚表示
- クリティカルチャージ蓄積量の表示
- 編成グリッドの空きセルをもっと目立たせる（「ここに配置」ガイド）
