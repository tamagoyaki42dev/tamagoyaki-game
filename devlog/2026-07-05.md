# 2026-07-05

## 敵HPバーの黒バー(背景)が緑バー(残HP)より短く見えるバグを修正

ユーザーから「敵のHPが削れると下に見える黒いバーが短い、全体のゲージ長さに合わせてほしい」と報告。

`battle_scene.gd` の頭上HPバーは背景(黒)と前景(緑・health_pctで減衰)の2枚のQuadMeshを重ねる構成で、両者のメッシュサイズ・position自体は完全に同一だった。原因はビルボード（常にカメラを向く処理）の実装方式の違い：

- 前景（`_hp_bar_shader` / `_HP_BAR_CODE`）：自前の頂点シェーダーで `length(MODEL_MATRIX[列])` を使い、親ノード（`ch`）のスケールを明示的に再適用してビルボード化
- 背景（`_make_bg_bar`）：Godot組込みの `StandardMaterial3D.billboard_mode = BILLBOARD_ENABLED` を使用

両者はスケール1の味方キャラでは一致するが、`enemy_char_scale * ed.model_scale`で等身大でない敵（ドラゴン等）だと、組込みビルボードとカスタムシェーダーのスケール再適用の挙動が食い違い、背景バーだけ小さく描画されるズレが発生していた。

修正：背景用に新しいシェーダー `_HP_BAR_BG_CODE`（前景と全く同じ頂点計算、フラグメントは`discard`なしで単色描画）を追加し、`_make_bg_bar()`をShaderMaterial化。前景・背景が同一のビルボード計算式を共有することで、スケールに関わらず常に同じ長さになるよう統一した。GUT全179テストPASS、実機でドラゴン等を殴ってバー長の一致を確認。

## 01:46 [skip-sns] 敵HPバーの黒背景がスケール違いで緑バーより短く見えるバグを修正

## 01:50 [skip-sns] プロト1完走と、難易度/育成/リリースを巡る所感をdevlogに記録

## リリース準備：アイコン差し替え・DEBUGボタン除去・初のWindowsビルド書き出し

プロト1完走を受け「次ならリリースの手順を」から着手。既存のリリース手順書は無かったため、Perrant（Godot 4.6.3 / Windowsデスクトップ / itch.io）の実態に沿って手順を起こしながら進めた。

- **`config/icon` がGodotデフォルトの青ロボットのままだった**と発覚（＝note第1弾で自虐ネタにした当のアイコン）。ユーザー提供の卵焼きロゴ（400×400 JPG）から、Pillowで `icon.png`（256²）と `assets/branding/perrant.ico`（16〜256pxマルチサイズ）を生成。`project.godot` を `res://icon.png` に差し替え、未参照になった `icon.svg`＋`.import` を削除。exeファイルアイコン用に `export_presets.cfg` の Windows プリセットへ `application/icon` を設定。ソースJPGは失くさないよう `assets/branding/perrant_icon_source_400x400.jpg` として保管。
- **タイトル画面のDEBUGボタンを削除**（`title_screen.gd`：ボタン生成ブロック＋専用定数 `_DEBUG_LAUNCHER_SCENE`）。公開版に開発導線を残さない。
- **エクスポートテンプレート未導入**が判明（`export_templates/` が空）。4.6.3-stable のテンプレ（1.25GB）を公式GitHubリリースからCLI取得し `4.6.3.stable/` へ設置。
- **初回書き出しが `export_filter="all_resources"` でGUT本体・`tests/` ごと同梱**していた（梱包中に `Cannot set object script` エラーも発生）。`exclude_filter="tests/*, addons/gut/*"` を追加して再書き出し＝エラー消滅・テストコード非同梱を確認。`tools/*` は `iso_preview.gd` がスクショ自動化のパスを実行時参照するため除外対象から外した。
- 生成物 `build/Perrant.exe`（208MB・単一ファイル・pck埋め込み）を実機起動＝Godot 4.6.3 / OpenGL 3.3 Compat で正常ブート・クラッシュなしをスモークテストで確認。`build/` は `.gitignore` に追加。

残タスク（backlog へ）：exeファイルアイコンがrcedit経由で実際に埋まったかの目視確認、ゲーム固有アイコン（今回のは「たまごやきGAMES」スタジオロゴ）を作るかの判断、itch.ioへのbutlerアップロード。

## noteネタ

### note第1弾を投稿 ―「素人がClaude Codeでモールモースリスペクトゲー開発 ～仕様書を読め～」

- URL: https://note.com/quiet_magpie2329/n/n7fb912836082
- 記念すべきnote第1弾。完成披露ではなく**開発初期のやらかし譚**を第1弾に選んだ（オープン開発の「ダサさ込みで見せる」路線）。

きっかけはユーザーの「開発初期の縦画面とか酷いSVGとか画像残ってないか」の一言。git履歴を発掘したら出てきたもの：

- **手書きSVGの闘技場背景** `assets/bg_arena.svg`（1344×1080・`feTurbulence`で石テクスチャ／トーチ／月光／城壁の凸凹まで全部手打ち）。なのに「背景は載せない判断」で今は宙に浮いた不遇の子。
- **icon.svg はいまだにGodotデフォルトのロボット顔**。一度も差し替えてない。
- **初バトル画面のシーン名が `scenes/node.tscn`**（ノード名も `Node`・unique_idもデフォルトのまま）。
- なお**縦画面時代はgit上に存在しなかった**。project.godotは初日から一貫して1920×1080横。ユーザーの縦画面の記憶はおそらくgit以前のGemini構想フェーズかitch.io用モックアップ。

白眉は **2026-06-13の自白**。devlogに本人（＝Claude）が「前半のコアは仕様書を読まずに書いたまがい物だったため、docs/3ファイルを全部読んでから作り直した」と残していた。**仕様書を書いた直後に、その仕様書を読まず、隣のGeminiとの会話履歴の記憶だけでバトル画面を組み**、同日夜に自白して全面作り直していた。作り直し前後で**見た目の骨格（紺地・分割線・カード・ログ位置）はほぼ据え置き**、変わったのは中身がゲーム仕様に合っているかだけ（3職→7人・defense撤去→攻補/防補・ボタン1個→ローテ/ステイ2択）。「まがい物と本物の差は絵じゃなくデータ構造に出る」が一枚絵で可視化できた。タイトルの「～仕様書を読め～」は、仕様書を無視したClaudeへのツッコミと、素人ユーザー自身への自虐の二重掛け。

Before/Afterを当時のソース座標から忠実復元してアーティファクト2枚を作成（初代6d3f1a4 / 作り直し4ba2daf）。初代→作り直しの所要は約4時間半。

### note用ブランディング素材を作成（見出し画像テンプレ＋ヘッダー）

note発信の見た目を固めた。いずれもHTML/CSSで組み、Chromeヘッドレス（`--screenshot --window-size`）で実寸PNGに焼く方式（Anthropicにテキスト→画像生成は無いため。むしろ本物のロゴ・日本語テキストをピクセル単位で正確に載せられるこの方式がヘッダー用途に向く）。全て `assets/branding/` に永続化。

- **note見出し画像テンプレ**（1280×670）：`note_thumb_template.html` → `note_thumb_shiyosho.png`。背景に初代バトル画面（6d3f1a4復元）をblur(2px)で沈め、「仕様書を読め」を白抜き190px1行でドン。回ごとに `.hero`/`.kicker`/連番だけ差し替えるブランド固定テンプレ。
- **noteヘッダー**（1920×1006）：`note_header_template.html` → `note_header.png`。スタジオロゴ（卵焼きバッジ）を主役に、金(#f2b63e)×紺×ピクセル火花で世界観を展開。コピーは名前の繰り返しを避け「つくる過程を、全部見せる。」＝オープン開発の看板に。左下はnoteアバター重なり想定で空けた。
- 出力先はプロジェクト内絶対パス必須（scratchpad/Tempはアクセス拒否0x5で書き出し失敗）。

**所感**：ヘッダー/サムネがスッと決まるのは制約が狭く（サイズ・ロゴ・配色固定）失敗が即焼き直せてノーリスクだから。ゲーム開発側で同じ大胆さが出しにくいのは、await/タイマー/シグナル連鎖やバランス数値が「壊すと高くつく」領域だから＝安全装置が効いているだけ。切り分けの整理として「ロジックは慎重に／UI・演出など見た目の面は大胆に」を今後の方針に。タイトル・クリア・敵情報画面のUIレイアウトや勝敗演出（backlogで「あっさりしすぎ」既出）は後者に該当。

### 朝ネタにした「Godotデフォルト青ロボアイコン」を、同日夜に成仏させた

上のnote第1弾で「icon.svg はいまだにGodotデフォルトのロボット顔。一度も差し替えてない」と自虐した、まさにその日の夜のリリース準備で、ついに卵焼きロゴへ差し替えた。朝に晒し者にした未修正の恥が、日をまたがず成仏した格好。ソース画像のファイル名が `xKWnFPvU_400x400.jpg`（X/Twitterのプロフィール画像そのままの謎ランダム名）で、ユーザーの第一声が実装依頼ですらなく「失くしそうだからどっかにコピーして名前も変えといて」だったのも味わい深い。なお差し替えたロゴは「たまごやきGAMES」のスタジオロゴであって、Perrantという作品固有のアイコンではない——スタジオ＝作品の一人開発ならではの、当面それで困らない省略。

### プロト1をitch＋Blueskyで公開しきった夜、ユーザーがこぼした一言

一連のリリース作業（アイコン差し替え→exeビルド→butlerでitch push→Bluesky告知）が全部通ったあと、ユーザーが実装と無関係にこぼした：「ほんの少しの一歩目だってわかってるけど、なんだかんだ嬉しいな」。規模を過大評価してはいない（本人が「一歩目」と自覚している）のに、それでも嬉しい、という素直な感情。頭や仕様書の中にしかなかったものが、その日から世界の誰かがDLして触れるものになった＝「触れる／触れない」の境界を越えた瞬間の実感。多くの個人開発が一度も越えずに終わる線を、3戦のプロトでも確かに越えた。この「わかってるけど嬉しい」はnote記事の締めや、オープン開発の等身大の記録としてそのまま使える一次感情。

## 12:23 [skip-sns] プロト1リリース準備：アイコンをスタジオロゴに差し替え・DEBUGボタン除去・Windowsビルド書き出し土台を整備

## 13:07 [skip-sns] リリース完了に伴いbacklog更新（exeアイコン確認・itchアップロードを完了に）

## 18:02 [skip-sns] note発信のブランディング素材整備とプロト2設計のトグル節昇格
