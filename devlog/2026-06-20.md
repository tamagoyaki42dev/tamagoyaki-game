# Devlog 2026-06-20

## 01:37 HPバーをシェーダーで右から減るよう修正。補助→攻撃の演出順序再生・列回復を横方向に修正

## 01:38 [skip-sns] ドキュメント・仕様書・CLAUDE.md を更新

## 01:38 [skip-sns] devlog/2026-06-20.md を追加

## セッション引き継ぎ作業

前セッション（2026-06-18）の変更をコミット・プッシュした。以下を実施：

- `docs/test_spec.md` B-6-9 の記述を「同縦列」→「後列の他キャラ（横方向）」に修正
- `README.md` プレイ環境・公開先をデスクトップ専用に更新
- `CLAUDE.md` アーキテクチャ原則・シーン配置・コーディング方針・演出方針を追記
- 全変更を origin/main にプッシュ（計12コミット分）

## 01:40 [skip-sns] devlog/2026-06-20.md にセッション引き継ぎ記録を追加

## テスト基盤整備（GUT導入）

- `scripts/battle/battle_math.gd` を新規作成。ダメージ・回復計算を純粋静的クラスに分離
- `battle_manager.gd` を BattleMath 経由に切り替え。`_front_col_empty` / `_get_mid_atk_support` / `_get_mid_def_support` を削除し RotationGrid メソッドに移動
- `rotation_grid.gd` に `has_front_unit_at_col` / `get_unused_atk_supporter_at_col` / `get_unused_def_supporter_at_col` を追加
- GUT 9.6.0 を導入し `tests/test_battle_math.gd`（18件）・`tests/test_rotation_grid.gd`（15件）を作成。全 33/33 Pass 確認
- `docs/visual_test_checklist.md` を作成

## 02:39 [skip-sns] テスト基盤を整備：BattleMath分離・RotationGrid整理・GUT導入（33件全Pass）

## HPバー色変化・シェーダー修正

- 右パネル ProgressBar：HP残量に応じて緑→黄→赤に変化（≥50% 緑 / <50% 黄 / <25% 赤）
- 3D HPバーシェーダー：同じ閾値で `bar_color` パラメータを更新
- **バグ修正**：`billboard` / `depth_draw_never` が Godot 4.6 Compatibility (OpenGL) レンダラーで非対応だったためHPバーが描画されていなかった。vertex 関数で MODELVIEW_MATRIX を手動操作するビルボードに置き換えて修正

## 02:56 HPバー色変化（緑→黄→赤）と3DシェーダーのCompatibility対応修正

## 演出追加セッション

- **死亡ユニット右パネルグレーアウト**：`_party_entries` で Panel を保持し、`_on_unit_died` でダーク背景スタイルに差し替え + HPバーをグレーに変更
- **Critical! テキストポップアップ**：クリット時に金色 Label3D がキャラ上で拡大→上昇→フェードアウト（`_spawn_critical_label`、`crit_font_size/duration/rise` で調整可）
- **回復フェーズを攻撃前に移動**：`battle_manager.gd` の回復処理順序を「攻撃後」→「攻撃前（2ターン目以降）」に変更。`battle_spec.md` も同期更新

## 次のセッションでやること

### 演出エフェクト（優先）
1. **攻撃補助エフェクト**（補助キャラがオレンジflash + 「攻撃補助」テキスト浮上）
2. **防御補助エフェクト**（補助キャラが青flash + 守護枠浮上）
3. **被弾flash色の攻撃タイプ別変化**（近接=赤・魔法=紫・遠距離=黄）
4. **「前/中/後」列ラベル表示**

### 演出化粧（演出土台完成後）
5. **ヒットストップ**（`Engine.time_scale=0.05` → タイマー → 1.0戻し）
6. **カメラシェイク**（`Camera3D h_offset/v_offset` を `FastNoiseLite` で制御）
7. **グロー/ブルーム**（`WorldEnvironment glow_enabled`）

### アート
8. **背景画像**（Kenney素材で戦場背景を設置）
9. **キャラ識別改善**（職業・陣営で外見差異。現状全員同モデル）

### ゲームシステム
10. **複数敵対応**
11. **3戦ゲームループ**（拠点画面・世代継承）
