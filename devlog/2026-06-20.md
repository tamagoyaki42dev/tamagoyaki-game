# Devlog 2026-06-20

## 01:37 HPバーをシェーダーで右から減るよう修正。補助→攻撃の演出順序再生・列回復を横方向に修正

## 01:38 [skip-sns] ドキュメント・仕様書・CLAUDE.md を更新

## 01:38 [skip-sns] devlog/2026-06-20.md を追加

## セッション引き継ぎ作業

前セッション（2026-06-18）の変更をコミット・プッシュした。以下を実施：

- `docs/test_spec.md` B-6-9 の記述を「同縦列」→「後列の他キャラ（横方向）」に修正
- `README.md` プレイ環境・公開先をデスクトップ専用に更新
- `CLAUDE.md` アーキテクチャ原則・シーン配置・コーディング方針・演出方針を追記
- 全変更を origin/main にプッシュ（計12コミット分）

## 01:40 [skip-sns] devlog/2026-06-20.md にセッション引き継ぎ記録を追加

## テスト基盤整備（GUT導入）

- `scripts/battle/battle_math.gd` を新規作成。ダメージ・回復計算を純粋静的クラスに分離
- `battle_manager.gd` を BattleMath 経由に切り替え。`_front_col_empty` / `_get_mid_atk_support` / `_get_mid_def_support` を削除し RotationGrid メソッドに移動
- `rotation_grid.gd` に `has_front_unit_at_col` / `get_unused_atk_supporter_at_col` / `get_unused_def_supporter_at_col` を追加
- GUT 9.6.0 を導入し `tests/test_battle_math.gd`（18件）・`tests/test_rotation_grid.gd`（15件）を作成。全 33/33 Pass 確認
- `docs/visual_test_checklist.md` を作成

## 02:39 [skip-sns] テスト基盤を整備：BattleMath分離・RotationGrid整理・GUT導入（33件全Pass）

## HPバー色変化・シェーダー修正

- 右パネル ProgressBar：HP残量に応じて緑→黄→赤に変化（≥50% 緑 / <50% 黄 / <25% 赤）
- 3D HPバーシェーダー：同じ閾値で `bar_color` パラメータを更新
- **バグ修正**：`billboard` / `depth_draw_never` が Godot 4.6 Compatibility (OpenGL) レンダラーで非対応だったためHPバーが描画されていなかった。vertex 関数で MODELVIEW_MATRIX を手動操作するビルボードに置き換えて修正

## 02:56 HPバー色変化（緑→黄→赤）と3DシェーダーのCompatibility対応修正

## 演出追加セッション

- **死亡ユニット右パネルグレーアウト**：`_party_entries` で Panel を保持し、`_on_unit_died` でダーク背景スタイルに差し替え + HPバーをグレーに変更
- **Critical! テキストポップアップ**：クリット時に金色 Label3D がキャラ上で拡大→上昇→フェードアウト（`_spawn_critical_label`、`crit_font_size/duration/rise` で調整可）
- **回復フェーズを攻撃前に移動**：`battle_manager.gd` の回復処理順序を「攻撃後」→「攻撃前（2ターン目以降）」に変更。`battle_spec.md` も同期更新

## 13:43 クリット時「Critical!」ポップアップ・死亡パネルグレーアウト・回復フェーズ順序修正

## 続きのセッション（同日）

### 実装したもの

- **攻撃補助エフェクト**：中衛→前衛にオレンジフラッシュ + 前衛頭上に「ATK Up」浮上。`supp_flash_color/duration`, `supp_label_color/offset` で調整可。GUT: `test_battle_scene_atk_support.gd`（5件）
- **防御補助エフェクト**：同パターンで青フラッシュ + 「DEF Up」。GUT: 同ファイル（計10件）
- **回復フラッシュエフェクト**：
  - 自回復：`unit_healed` で緑フラッシュ + 数字同時表示
  - 列回復：右（高col）→左の順に `heal_row_stagger`（0.15s）ずつずらして順番表示
  - 複数ヒーラーはキューで直列処理（`_row_heal_animating` フラグ）、ヒーラー間に `heal_batch_gap`（0.65s）
  - GUT: `tests/test_battle_scene_heal.gd`（5件）
- **フェーズ同期**：列回復が終わるまで攻撃フェーズが始まらないよう `row_heal_anim_done` シグナルで同期。`_apply_row_healing` が `await row_heal_anim_done`、シーンのキュー処理完了時に emit
- **post_update.py 改修**：確認をtkinter GUIダイアログに変更、スクショのmtime検証（コミット時刻+30s）
- **F12スクショ機能**：`battle_scene.gd` の `_unhandled_input` で `res://tools/screenshot.png` に保存
- **右パネル キャラ画像スペース**：HPバーの左に56px プレースホルダー復元

### 設計上の重要な決定

- `static func _make_*_label` パターン：BattleScene をインスタンス化せずにGUTテストできるラベル生成の標準パターン
- `_do_flash(ch, color, duration)` ：カラー・デュレーション引数付きに拡張（デフォルトは赤・0.08s）
- フェーズカプセル化の原則を確認：「固定タイマーで十分だろう」ではなく「シグナルで完了を保証する」が正しい設計。**列回復のみ対応済み、他フェーズは設計負債として backlog に積んだ**

### 設計負債（backlog 追加済み）

全フェーズのシグナルベース同期：`rotate_anim_done` / `self_heal_anim_done` / `unit_action_anim_done` を追加し、`ROTATE_DELAY` / `RECOVERY_DELAY` / `ACTION_DELAY` の固定タイマーを削除。表示のための「見せ時間」はシーン側で管理する。

## 17:26 攻撃補助・防御補助・回復フラッシュエフェクト追加、SNS投稿フロー改善

## 被弾フラッシュ色の攻撃タイプ別変化

- `_resolve_hit_flash_color` 静的関数を追加。攻撃者の職業から近接/遠距離/魔法を判定し `_do_flash` に渡す色を切り替える
- `hit_flash_melee_color`（赤）/ `hit_flash_magic_color`（紫）/ `hit_flash_ranged_color`（黄）を `@export` で調整可能に
- 敵攻撃は職業データなしのため一律近接（赤）扱い
- 視覚的な差は控えめ。フラッシュ以外の攻撃タイプ演出は後で検討
- GUT: `tests/test_battle_scene_hit_flash.gd`

## 17:45 [skip-sns] 被弾フラッシュ色を攻撃タイプ別に変更（近接=赤・魔法=紫・遠距離=黄）

## 戦場レイアウト表示・UIレイアウト @export 化・HP定数統一

- **前/中/後 列ラベル**：`_make_row_label` 静的関数で Label3D を PlayerGrid に追加。billboard + no_depth_test
- **4×3グリッドタイル**：`_make_grid_cell` 静的関数で PlaneMesh + 半透明 StandardMaterial3D を各スロットに配置
- **PlayerGrid 位置調整**：`transform = (-0.899, 0, 7.187)` でキャラとグリッドを画面中央左に配置
- **敵スロット位置調整**：ESlot_0 を `(0.823, 0, 0.485)` に移動
- **キャラスケール @export 化**：`player_char_scale` / `enemy_char_scale` を追加（現在 2x / 4x）
- **BattleUI レイアウト @export 化**：ボタン位置・サイズ・カウントダウン位置・アクションパネル等を全て ratio/px の @export に
- **HP 定数重複解消**：`BattleUI` の `HP_*` 定数を削除し `BattleScene.HP_*` への直接参照に統一（`/code-review` で発見）
- GUT: `tests/test_battle_scene_grid.gd`

## 19:19 戦場グリッド・列ラベル表示、UIレイアウト @export 化、HP定数統一

## 19:33 [skip-sns] /simplify: Label3D生成を共通ベースファクトリに統合・二重ルックアップ解消

## 設計負債解消（battle_scene.gd）

- **`_animate_floating_label` 抽出**：`_spawn_atk_support_label` / `_spawn_def_support_label` の同一Tweenアニメ14行×2を共通ヘルパーに統合
- **`_process_row_heal_queue` を while ループに変換**：末尾再帰 → `while not _row_heal_queue.is_empty()` に変更。再帰中に新バッチが来ても同ループで処理
- **バグ修正**：while ループ変換で最終バッチ後にも `heal_batch_gap`（650ms）が走る回帰バグが `/code-review` で発覚。最終イテレーションをスキップするガードを追加
- **`_spawn_damage_label` → `_spawn_floating_label` にリネーム**：ダメージ以外（回復・クリット）にも使うため
- **`const NEXT_SCENE` / `@export battle_end_delay` 追加**：`_on_battle_ended` のマジックリテラルを除去
- **`/code-review` 頻度方針を CLAUDE.md に追加**：毎コミット自動実行しない・条件を満たしたときに提案する形に変更

## 20:00 [skip-sns] battle_scene.gd 設計負債解消：Tweenヘルパー抽出・while ループ変換・heal_batch_gap 回帰バグ修正

## 次のセッションでやること

### 演出化粧（優先）
1. **ヒットストップ**（`Engine.time_scale=0.05` → タイマー → 1.0戻し）
2. **カメラシェイク**（`Camera3D h_offset/v_offset` を `FastNoiseLite` で制御）
3. **グロー/ブルーム**（`WorldEnvironment glow_enabled`）

### 設計負債（タイミングを見て）
4. **フェーズ完全カプセル化**：全フェーズをシグナルベースのawaitに統一（`rotate_anim_done` / `self_heal_anim_done` / `unit_action_anim_done` を追加、固定タイマー削除）

### アート
5. **背景画像**（Kenney素材で戦場背景を設置）
6. **キャラ識別改善**（職業・陣営で外見差異。現状全員同モデル）

### ゲームシステム
7. **3戦ゲームループ**（拠点画面・世代継承）
