# Devlog 2026-06-21

## ヒットストップ実装

- `hitstop_time_scale`（0.05）/ `hitstop_duration`（0.08s）を `@export` で追加
- `_do_hitstop()`：`Engine.time_scale` を下げ、`ignore_time_scale=true` のリアルタイムタイマーで 1.0 に戻す
- GUT: `tests/test_battle_scene_hitstop.gd`（2件）

## カメラシェイク実装

- `shake_intensity`（0.05）/ `shake_crit_intensity`（0.12）/ `shake_duration`（0.25s）を `@export` で追加
- `_do_camera_shake(intensity)`：`FastNoiseLite` でフレームごとに `h_offset` / `v_offset` を更新、フェードアウト後に 0 リセット。`_shake_active` フラグで多重起動防止
- `_do_hit_effects(is_crit)`：ヒットストップ終了直後にシェイクを起動するチェーン関数
- `_on_unit_acted()` の `_do_hitstop()` 呼び出しを `_do_hit_effects(is_crit)` に差し替え
- GUT: `tests/test_battle_scene_hitstop.gd` に3件追加（計6件）＋クリット強度 > 通常強度の検証1件

## 09:09 [skip-sns] ヒットストップ＋カメラシェイク追加：被弾時に全画面スロー→カメラ揺れでヒット感強化

## グロー/ブルーム実装

- `scenes/battle.tscn` の `WorldEnvironment` に `Environment` サブリソースを追加
- `glow_enabled = true`、`glow_bloom = 0.5`（SDR範囲の被弾フラッシュにも滲み）、`glow_blend_mode = 1`（Screen）
- 背景色を `Color(0.05, 0.05, 0.1)` に統一（スクリプト側と同じ暗紺）
- テストなし（エディタ設定のみで完結）

## フェーズ完全カプセル化：ローテーション（1/3）

- `BattleManager` に `signal rotate_anim_done` を追加（`row_heal_anim_done` と同パターン）
- `ROTATE_DELAY` 定数（0.9s 固定）を削除 → `await rotate_anim_done` に置換
- `BattleScene._on_rotated()` を async 化：`await tw.finished → await timer(rotate_show_duration=0.35s) → emit`
- `moves.is_empty()` 時は即 emit（デッドロック防止）
- GUT `test_battle_scene_rotate_anim.gd`（3件）追加
- `test_battle_manager_flow.gd` の `rotated` コールバックで `call_deferred` により `rotate_anim_done` を emit（同期 emit だと await より先に発火するため）

## 自己回復フェーズをシグナルベース化

- `BattleManager` に `signal self_heal_anim_done` を追加（`rotate_anim_done` と同パターン）
- `RECOVERY_DELAY` 定数（1.0s 固定）を削除 → `await self_heal_anim_done` に置換
- `BattleScene._on_phase_started(phase)` を追加：`"recovery"` 受信 → `await timer(self_heal_show_duration=1.0s)` → emit
- `_start_battle()` に `phase_started.connect(_on_phase_started)` 追加
- `@export var self_heal_show_duration: float = 1.0` 追加
- GUT `tests/test_battle_scene_self_heal.gd`（1件）追加
- `test_battle_manager_flow.gd` の `phase_started` コールバックで `recovery` 時に `call_deferred` emit

## 列回復後の見せ時間追加

- `@export var post_row_heal_show_duration: float = 0.5` を追加
- `_process_row_heal_queue` の末尾、`row_heal_anim_done.emit()` 直前に `await timer(0.5s)` を挿入
- 列回復アニメ完了→攻撃フェーズ移行までの間が短すぎたため調整

## 09:46 [skip-sns] 自己回復フェーズをシグナルベース化：RECOVERY_DELAY削除・self_heal_anim_done追加・列回復後の見せ時間0.5s追加

## ユニット行動フェーズ シグナルベース化

- `BattleManager` に `signal unit_action_anim_done` を追加
- `ACTION_DELAY` 定数（1.0s 固定）を削除 → `await unit_action_anim_done` に置換（2箇所）
- `BattleScene._on_unit_acted()` に `_unit_action_anim_pending` フラグを追加：多段ヒット（DOUBLE等）で二重 emit を防止
- 攻撃 Tween 完了 → `unit_action_show_duration`（0.6s）待機 → emit
- `@export var unit_action_show_duration: float = 0.6` 追加
- GUT `tests/test_battle_scene_unit_action_anim.gd`（1件）追加

## CHARGE/ノーターゲット デッドロック修正

- `_unit_acted_this_action: bool` フラグを追加
- `_do_single_hit` の `unit_acted.emit()` 直前でフラグをセット
- `advance_turn` でフラグを監視：`false` ならマネージャー自身が `unit_action_anim_done.emit()` で即アンロック
- 対象：CHARGE アクション・ターゲットなしケース

## ROW 全体攻撃 防御補助 overlap 修正

- 従来：ターゲット1体ずつ順番に補助発動→SUPPORT_DELAY→攻撃を繰り返す → 2体目の補助と1体目の攻撃が同時になる
- 修正後：全対象の補助を一括 emit → SUPPORT_DELAY 1回待機 → 全員攻撃
- `_do_single_hit` に `pre_applied_def_support: int = -1` を追加（>= 0 なら内部検出スキップ）

## ローテーション演出改善：着地スカッシュ

- 移動 Tween 完了直後、全キャラに Y 縮小・XZ 拡大 → 元スケールに戻るスカッシュアニメを追加
- `@export` 4件：`landing_squash_y`（0.65）・`landing_squash_xz`（1.2）・`landing_squash_in_time`（0.06s）・`landing_squash_out_time`（0.18s）
- GUT `tests/test_battle_scene_landing_squash.gd`（4件）追加

## ローテーション演出改善：到着フラッシュ

- スカッシュと同タイミングで全キャラに `_do_flash()` を呼び出し（青白い光）
- `@export` 2件：`arrive_flash_color`（Color(0.7, 0.85, 1.0)）・`arrive_flash_duration`（0.4s）
- GUT `tests/test_battle_scene_arrive_flash.gd`（2件）追加

## ローテーション演出改善：完了時列フラッシュ

- スカッシュ完了後、前列（`unit.row == 0`）のキャラのみゴールドフラッシュ（到着フラッシュより 0.15s 遅れ）
- `@export` 3件：`front_row_flash_color`（Color(1.0, 0.85, 0.3)）・`front_row_flash_duration`（0.45s）・`front_row_flash_delay`（0.15s）
- GUT `tests/test_battle_scene_front_row_flash.gd`（3件）追加

## キャラアニメ統合（idle / attack / die）

- `_unit_anims: Dictionary`（BattleUnit → AnimationPlayer）を追加、スポーン時に populate
- **idle**：`_spawn_char` で `loop_mode = Animation.LOOP_LINEAR` をセットしてループ再生（デフォルト LOOP_NONE を上書き）
- **attack**：`_on_unit_acted` の攻撃 Tween を廃止 → GLB 内蔵アニメ `attack-melee-right` / `attack-melee-left` に置換。`animation_finished` を await して idle に戻す
- **die**：`_on_unit_died` で GLB 内蔵 `die` アニメを再生。取得失敗時は Tween フォールバック
- 攻撃 Tween 関連 `@export`（`atk_wind_up_dist` 等）を削除、代わりに `unit_action_show_duration` を追加
- CLAUDE.md・visual_spec.md のグラフィック方針を Kenney GLB ベースに更新

## 被弾火花パーティクル追加

- `_spawn_hit_sparks(pos, is_crit)` を追加（`_spawn_death_particles` と同パターン、GPUParticles3D）
- `_on_unit_acted` の `_do_hit_effects` 直後に fire-and-forget 呼び出し
- 通常・クリットで色・量・スケールを差別化：`spark_amount=28` / `spark_crit_amount=48`
- `@export` 11件：`spark_color` / `spark_crit_color` / `spark_scale_*` / `spark_crit_scale_*` / `spark_velocity_*` / `spark_lifetime`
- 色が反映されない問題を修正：`ShaderMaterial` + `COLOR` ビルトイン直接参照で解決（`StandardMaterial3D.vertex_color_use_as_albedo` では Godot 4 で確実に動かないため）
- グラデーションの重複ポイントバグを修正：`add_point(1.0, ...)` → `set_color(1, ...)`
- GUT `tests/test_battle_scene_hit_sparks.gd`（6件）追加

## 攻撃エフェクトタイミング調整

- `@export var attack_impact_delay: float = 0.2`（秒）を追加
- アニメ開始から `attack_impact_delay` 後に被弾エフェクト（flash・火花・ノックバック）を発火
- `unit_action_anim_done` emit タイミングへの影響なし（`animation_finished` await は遅延分アニメが進んだ状態から計測）
- GUT `tests/test_battle_scene_attack_impact_delay.gd`（1件）追加

## 次のセッションでやること

### アート
- **横揺れ修正**（ノックバック時の Z 軸ランダム移動をゼロにする）
- **背景画像**（Kenney素材で戦場背景を設置）
- **キャラ識別改善**（職業・陣営で外見差異、現状全員同モデル）

### ゲームシステム
- **3戦ゲームループ**（拠点画面・世代継承）
