# tamagoyaki-game — Claude Code 向け説明

## プロジェクト概要

**スタジオ:** たまごやきゲームズ  
**ジャンル:** ローテーションバトル × 世代継承ローグライト × 闘技場ハクスラ  
**エンジン:** Godot 4（標準版・GDScript）  
**公開先:** itch.io（HTML5エクスポート）

## ゲームコンセプト

「完全情報開示の詰将棋 × 死んでも強くなるローグライト × 闘技場ハクスラ」

モールモースの騎兵隊 / V&B のローテーションバトルシステムにインスパイア。  
マイナーセブンとの差別化：極振りエネミー・死の恩恵・超高速ハクスラ。

## 設計思想（引き算）

- マップ移動なし（拠点 ⇄ 遠征 ⇄ 世代交代の3画面ループのみ）
- アニメーションなし（ColorRect + スクリーンシェイク）
- 常時通信なし（完全オフライン）
- インベントリ画面なし（装備はキャラ数値に直接加算・消滅）

## 技術スタック

| レイヤー | 技術 |
|---|---|
| ゲームエンジン | Godot 4 / GDScript |
| データ管理 | Godot Resource + JSON（user://save.json） |
| ツール系 | Python |
| CI/CD | GitHub Actions |

## UI・操作方針

- **横画面（1920×1080）**で設計・開発。スマホ対応は後回し
- **マウス左クリック（タップ）のみ**で全操作完結
- キーボードショートカットは後付けオプション
- データパスは必ず `user://` を使う（絶対パス禁止）

## セッション開始時に必ずやること

1. `devlog/` の最新日付ファイルを読む → 「次のセッションでやること」を把握する
2. ユーザーへの確認なしに即座に作業を開始できる状態にする

## 仕様書

戦闘・敵・ユニット関連の実装・修正を行う際は、作業前に該当ファイルを参照すること。

| ファイル | 参照タイミング |
|---|---|
| `docs/battle_spec.md` | 戦闘ロジック・ローテーション・ダメージ計算・勝敗条件に触れるとき |
| `docs/enemy_spec.md` | 敵生成・敵の行動・思考タイプに触れるとき |
| `docs/unit_spec.md` | 職業・ステータス数値・バランスに触れるとき |

## ファイル構成

```
tamagoyaki-game/
├── docs/          # 仕様書
├── scenes/        # Godot シーン
├── scripts/       # GDScript
├── tools/         # Python ツール（自動投稿等）
├── devlog/        # 開発日誌（GitHubに公開）
├── .env           # APIキー（gitignore済み・コミット禁止）
├── .env.example   # キーのテンプレート
└── CLAUDE.md      # このファイル
```

## コミット・SNS投稿フロー

コミット前に以下を順番に確認・更新してからコミットメッセージ案を提案する：

1. **`devlog/今日の日付.md`** — 末尾に `## 次のセッションでやること` セクションを書く
2. **`README.md`** — 技術スタック・プレイ環境が実態と一致しているか確認・修正
3. **`CLAUDE.md`** — 設計方針の変更が反映されているか確認・修正

編集したファイルは必ず読み直して構造が壊れていないか確認してからコミットメッセージ案を提案する。

### SNS投稿の判断ルール（私が自動で判断する）

**投稿する**（SNS映えする文体で書く）:
- 新機能・新画面の追加
- ゲームとして動作が変わるバグ修正
- ビジュアルの変化（UI改善など）

**投稿しない**（コミットメッセージ末尾に `[skip-sns]` を付ける）:
- ドキュメント更新（CLAUDE.md / README.md / devlog / 仕様書）
- 設定ファイルの変更（project.godot など）
- リファクタリング・コード整理
- インフラ・ツール整備
- 仕様ズレの軽微な修正

投稿する場合はコミットメッセージがそのまま X・Bluesky への投稿文になるため、SNS映えする文体（日本語・絵文字なし・ハッシュタグはスクリプトが付ける）で書く。

## コーディング方針

- コメントは「なぜそうしたか」が自明でない場合のみ書く
- GDScript は Godot 公式スタイルガイドに従う
- セキュリティ：APIキー等は必ず .env から読む。ハードコード禁止

## このファイルの更新ルール

**設計方針が変わったら、その場でこのファイルも更新する。コミットより先に。**

- 技術選定・UI方針・画面設計が変わったら該当箇所を書き換える
- 「後で直す」はしない。古い情報が残るとセッション切れ後に誤実装の原因になる
- 仕様書（docs/）の内容はここに転記しない。参照ルールだけ書く
