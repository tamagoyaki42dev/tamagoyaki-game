# tamagoyaki-game — Claude Code 向け説明

## 情報の置き場所（4ファイルの使い分け）

迷ったらこの表に従う。判定軸は「時制」と「対象」。

| ファイル | 何を持つ | 完了/変化したら |
|---|---|---|
| `docs/backlog.md` | これからやること（未来・タスク） | 消す |
| `devlog/今日の日付.md` | やったこと・決定・noteネタ（過去・記録） | 残る（日付で積む） |
| `CLAUDE.md`（この file） | どう作るか（不変ルール・全員適用の作業規約） | 書き換える |
| memory | ユーザーの好み・私の行動補正（私固有の学習） | 上書き/削除 |

**CLAUDE.md と memory の判定テスト**：「他の誰がこのリポジトリで作業しても従うべきルールか？」
→ Yes は CLAUDE.md（リポジトリに残る）／ No（ユーザー個人の好み・私の癖補正）は memory。**迷ったら CLAUDE.md 優先。**

## プロジェクト概要

**スタジオ:** たまごやきゲームズ  
**ジャンル:** ローテーションバトル × 世代継承 × ローグライク  
**エンジン:** Godot 4 / GDScript  
**公開先:** itch.io / Steam（デスクトップビルドのみ）

## ゲームコンセプト

**ローテーションバトル × 世代継承 × ローグライク**

2つの設計原則を軸にする：

1. **面白い戦闘** — 原作（モールモース/V&B）のローテーションバトルを核とする
2. **投資が形になる** — 時間をかけたものが強さになり蓄積する。世代継承でキャラへの愛着も積み上がる。負けは終わりでなく次世代への布石（滅亡→再興のローグライクループで包む＝内は継承、外はリセット）

モールモースの騎兵隊 / V&B のローテーションバトルシステムにインスパイア。内/外構造の詳細は `docs/game_concept.md`。

※「整理整頓・工場化」（素材加工→自動化）は3本目の柱から降ろし当面凍結（2026-07-03）。遠い将来の構想には残すが今は忘れてよい。

## 設計思想（引き算）

- マップ移動なし（拠点 ⇄ 遠征 ⇄ 世代交代の3画面ループのみ）
- 演出はシンプルに保つ（Tween + Shader。複雑なアニメーション制御は入れない）
- 常時通信なし（完全オフライン）
- インベントリ画面なし（装備はキャラ数値に直接加算・消滅）

## UI・操作方針

- **横画面（1920×1080）**で設計・開発。スマホ対応は後回し
- **マウス左クリック（タップ）のみ**で全操作完結
- データパスは必ず `user://` を使う（絶対パス禁止）

## アーキテクチャ原則（What goes where）

- **データ**（ユニット・敵・形質・アイテム）→ カスタムResource（.tres）
- **全体状態**（世代・文明・進行）→ Autoload（シングルトン）
- **連絡** → Signal（イベント通知はSignal。UIが制御する方向の呼び出しは直接参照で可）
- **調整値**（速度・大きさ・時間・確率）→ 必ず `@export` か先頭 `const`
- **画面・メニュー・ログ** → CanvasLayer（2D）

## シーン・配置

- 戦場は全面3D（Node3D + Camera3D アイソメ固定、SubViewportは戦場全体で1つ）
- キャラ配置はMarker3D基準。座標のコード直書き禁止
- 再利用単位は1シーン（味方・敵は共通BattleUnit）

## コーディング方針

- 静的型付け必須（`var x = ...` 禁止・型を明示）
- 変数・関数：snake_case / 定数：UPPER_CASE / class_name：PascalCase
- 1スクリプト1責務。2責務以上になりそうなら相談する
- 調整値は `@export` か先頭 `const`

## 進め方

- 新システム追加・既存構造の変更は設計案を先に出し、承認後に実装する
- アニメ名・既存仕様が不明なら憶測せず質問する
- これはプロトタイプ（3戦、基本戦闘と編成画面のみ）。凝った仕様は作りこまない
- 演出は1個ずつ足す。複数同時追加禁止

## グラフィック方針

- アセットはローポリCC0で統一。味方・背景は Kenney。Kenney に不足する敵モンスターは Quaternius（CC0・同系統ローポリ）を併用してよい
- 人型モーション = Kenney GLB 内蔵アニメ（idle / attack-melee-right / attack-melee-left / die ほか計32種）。Mixamo は不要
- 非人型 = モデルに内蔵アニメがある場合は内蔵アニメをフル活用する（idle / death / 攻撃など）。内蔵に該当アニメが無い動作（攻撃アニメ無しの敵の攻撃など）のみ Tween で補う（例：1歩前進→戻る突進）

## 助言義務

- 些末でない依頼は実装前に「使うノード/機能と選んだ理由」を一言添える
- 「それGodotにXという標準機能がある」と気づいたら必ず指摘する
- **`await`・タイマー・Signal の連鎖を含むコードを変更するときは、実装前に必ず明示する：**
  「この変更はテストで保護されません。実装後に〔具体的な動作〕を手動で確認してください」
- **ビジュアル・演出の変更を含む実装プロンプトには必ず両方を記載する：**
  1. GUTテスト（プロパティ値の検証）
  2. 目視確認手順（何をどう見るか具体的に）

## セッション開始時に必ずやること

最新 devlog と `docs/backlog.md` は SessionStart フック（`tools/session_start_hook.py`）が自動注入する。注入内容を読んだ上で、ユーザーへの確認なしに即座に作業を開始できる状態にする。注入が見当たらない場合（フック失敗）は両ファイルを手動で読む。

## タスク管理

- **`docs/backlog.md` が唯一のタスク台帳**（永続・セッション開始時にロード）。未着手タスクはすべてここに集約する
- **backlog に置くのは「直近の着手可能タスク」のみ。** 将来の設計方針・長文の決定事項は `docs/` の設計書（プロト2以降は `docs/proto2_design.md`）へ移し、backlog には1行ポインタだけ残す（自動ロードされる台帳が肥大すると直近タスクが埋もれる）
- **タスクを追加・完了したら、その瞬間に `backlog.md` を編集する。** コミットや区切りまで溜めない（溜めると必ず漏れる）
- **TodoWrite は backlog から切り出した「今やっている分」の作業ビュー。** 新タスクは backlog に先に書いてから TodoWrite に載せる。TodoWrite だけで未来のタスクを抱えない
- devlog は物語ログ（やったこと・決定・noteネタ）。次にやることは backlog に集約し、devlog に複製しない
- **「道具を作る」と「道具で確かめる」は別タスク。** ツール（シミュ・生成器・検証ハーネス等）の完了条件は、**それを回して得た結果**（構成を変えた分布・サマリ）に置く。「ツールが在る／自分のテストが緑」はマイルストーンでありゴールではない。**GUT全Pass＝コードが壊れていない、であって設計検証済みではない。** バックグラウンドエージェントの「完了」報告も同じ＝スコープ内（ビルド）の完了であり運用・検証の完了ではない。仕様書の完了条件表に assert で書ける項目と書けない運用項目が混在していたら、後者は必ず別タスクとして backlog に切り出す

## 仕様書

実装・修正を行う際は、作業前に該当ファイルを参照すること。

| ファイル | 参照タイミング |
|---|---|
| `docs/battle_spec.md` | 戦闘ロジック・ローテーション・ダメージ計算・勝敗条件に触れるとき |
| `docs/enemy_spec.md` | 敵生成・敵の行動・思考タイプに触れるとき |
| `docs/unit_spec.md` | 職業・ステータス数値・バランスに触れるとき |
| `docs/visual_spec.md` | 戦闘演出・グラフィック実装に触れるとき |
| `docs/game_concept.md` | ゲームの根幹思想・内外構造に触れるとき |
| `docs/proto2_design.md` | プロト2以降の設計・方針（個体値・手続き生成・敵行動乱数・必殺ボタン・KayKit移行）に触れるとき |
| `docs/godot_feature_map.md` | 使うべきGodot機能を確認するとき |
| `docs/design_decisions.md` | 既存メカの意味・トリガー・発動条件に触れるとき（＝「触ると死ぬ配線」＝設計判断の照合。balance-verify スキルが作業前に自動照合） |

## プロジェクトスキル（.claude/skills/）

作業手順のうち「発動タイミングが特定できるもの」はスキルに置く（このファイルには常時適用のルールだけ残す）。発動条件に該当したら必ず対応スキルを起動して従う。

| スキル | 発動条件 |
|---|---|
| `commit-flow` | コミット・コミットメッセージ提案・SNS投稿のとき（必須） |
| `balance-verify` | バランス数値・ダメージ計算・シムの実行/解釈・仕様書の数値更新・戦闘ロジック土台ファイルの改修に触れるとき（前提照合＋数値照合の2段） |
| `audit` | 構造監査を依頼されたとき |

`balance-verify` と `audit` には**検証エージェント（独立検算）**が組み込まれている：結論がまとまったら、結論と参照ファイルパスだけ（導出過程は渡さない）を別エージェントに渡して再計算・実在確認させる。頻度は結論セット単位で1回。

## noteネタの記録ルール

セッション中に「面白い・珍しい・笑える」出来事が起きたら、その場で `devlog/今日の日付.md` の `## noteネタ` セクションに追記する。

**記録対象の判断基準：**
- 予想外の失敗・勘違い・誤解（特にグラフィック系のやらかし）
- ユーザーとのやり取りで笑えた・ためになった会話
- 「なぜそうなった」が後から読んで面白いエピソード
- 感動した瞬間・うまくいって嬉しかった出来事

記録タイミングはその場で。「後でまとめる」はしない。

**検知のヒント：** ユーザーが実装と無関係な話を切り出してきたタイミング＝何か感じたことがある。そのやり取りが終わったら確認せずdevlogに下書きしておく。

## コミット・SNS投稿フロー

手順の全文は `commit-flow` スキル（`.claude/skills/commit-flow/SKILL.md`）にある。**コミットメッセージを提案する前に必ずスキルを起動して従う**（GUTフルラン・devlog/backlog/仕様書/README同期・SNS投稿判断・定期整理フロー・`/code-review` 推奨基準を含む）。

## このファイルの更新ルール

**設計方針が変わったら、その場でこのファイルも更新する。コミットより先に。**

- 技術選定・UI方針・画面設計が変わったら該当箇所を書き換える
- 「後で直す」はしない。古い情報が残るとセッション切れ後に誤実装の原因になる
- 仕様書（docs/）の内容はここに転記しない。参照ルールだけ書く
