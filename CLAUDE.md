# tamagoyaki-game — Claude Code 向け説明

## プロジェクト概要

**スタジオ:** たまごやきゲームズ  
**ジャンル:** ローテーションバトル × 世代継承 × 拠点整備  
**エンジン:** Godot 4 / GDScript  
**公開先:** itch.io / Steam（デスクトップビルドのみ）

## ゲームコンセプト

**ローテーションバトル × 世代継承 × 拠点整備**

3つの設計原則を軸にする：

1. **面白い戦闘** — 原作（モールモース/V&B）のローテーションバトルを核とする
2. **投資が形になる** — 時間をかけたものが強さになり蓄積する。世代継承でキャラへの愛着も積み上がる
3. **整理整頓・工場化** — 素材加工→組み合わせ→自動化のループ。戦闘報酬で拠点を整備し部隊を強化する

モールモースの騎兵隊 / V&B のローテーションバトルシステムにインスパイア。

## 設計思想（引き算）

- マップ移動なし（拠点 ⇄ 遠征 ⇄ 世代交代の3画面ループのみ）
- 演出はシンプルに保つ（Tween + Shader。複雑なアニメーション制御は入れない）
- 常時通信なし（完全オフライン）
- インベントリ画面なし（装備はキャラ数値に直接加算・消滅）

## UI・操作方針

- **横画面（1920×1080）**で設計・開発。スマホ対応は後回し
- **マウス左クリック（タップ）のみ**で全操作完結
- データパスは必ず `user://` を使う（絶対パス禁止）

## アーキテクチャ原則（What goes where）

- **データ**（ユニット・敵・形質・アイテム）→ カスタムResource（.tres）
- **全体状態**（世代・文明・進行）→ Autoload（シングルトン）
- **連絡** → Signal（イベント通知はSignal。UIが制御する方向の呼び出しは直接参照で可）
- **調整値**（速度・大きさ・時間・確率）→ 必ず `@export` か先頭 `const`
- **画面・メニュー・ログ** → CanvasLayer（2D）

## シーン・配置

- 戦場は全面3D（Node3D + Camera3D アイソメ固定、SubViewportは戦場全体で1つ）
- キャラ配置はMarker3D基準。座標のコード直書き禁止
- 再利用単位は1シーン（味方・敵は共通BattleUnit）

## コーディング方針

- 静的型付け必須（`var x = ...` 禁止・型を明示）
- 変数・関数：snake_case / 定数：UPPER_CASE / class_name：PascalCase
- 1スクリプト1責務。2責務以上になりそうなら相談する
- 調整値は `@export` か先頭 `const`

## 進め方

- 新システム追加・既存構造の変更は設計案を先に出し、承認後に実装する
- アニメ名・既存仕様が不明なら憶測せず質問する
- これはプロトタイプ（3戦、基本戦闘と編成画面のみ）。凝った仕様は作りこまない
- 演出は1個ずつ足す。複数同時追加禁止

## グラフィック方針

- アセットは全部Kenneyの同じファミリーで統一（敵・背景も）
- 人型モーション = Mixamo / 非人型 = Godotアニメ

## 助言義務

- 些末でない依頼は実装前に「使うノード/機能と選んだ理由」を一言添える
- 「それGodotにXという標準機能がある」と気づいたら必ず指摘する
- **`await`・タイマー・Signal の連鎖を含むコードを変更するときは、実装前に必ず明示する：**
  「この変更はテストで保護されません。実装後に〔具体的な動作〕を手動で確認してください」
- **ビジュアル・演出の変更を含む実装プロンプトには必ず両方を記載する：**
  1. GUTテスト（プロパティ値の検証）
  2. 目視確認手順（何をどう見るか具体的に）

## セッション開始時に必ずやること

1. `devlog/` の最新日付ファイルを読む → 「次のセッションでやること」を把握する
2. ユーザーへの確認なしに即座に作業を開始できる状態にする

## 仕様書

実装・修正を行う際は、作業前に該当ファイルを参照すること。

| ファイル | 参照タイミング |
|---|---|
| `docs/battle_spec.md` | 戦闘ロジック・ローテーション・ダメージ計算・勝敗条件に触れるとき |
| `docs/enemy_spec.md` | 敵生成・敵の行動・思考タイプに触れるとき |
| `docs/unit_spec.md` | 職業・ステータス数値・バランスに触れるとき |
| `docs/visual_spec.md` | 戦闘演出・グラフィック実装に触れるとき |
| `docs/game_concept.md` | ゲームの根幹思想・内外構造に触れるとき |
| `docs/godot_feature_map.md` | 使うべきGodot機能を確認するとき |
| `docs/workflow.md` | 監査・機能依頼のテンプレートが必要なとき |

## コミット・SNS投稿フロー

コミット前に以下を順番に確認・更新してからコミットメッセージ案を提案する：

0. **コミット前に必ずユーザーに確認する（この確認なしにコミットメッセージを提案しない）**
   - 「GUT の全テストは Pass しましたか？」
   - 以下の2つは**視覚変化・ゲームプレイ変化があるコミットのみ**確認する：
     - 「`docs/visual_test_checklist.md` の全項目を確認しましたか？」
     - 「1戦を通してプレイし、不自然な挙動がないことを確認しましたか？」
   → 上記が完了してからコミットメッセージを提案する

1. **`devlog/今日の日付.md`** — 末尾に `## 次のセッションでやること` セクションを書く
2. **`docs/battle_spec.md` / `docs/enemy_spec.md` / `docs/unit_spec.md`** — 今回の実装・決定で仕様書と乖離が生じていないか確認・更新
3. **`README.md`** — 技術スタック・プレイ環境が実態と一致しているか確認・修正
4. **`CLAUDE.md`** — 設計方針の変更が反映されているか確認・修正

編集したファイルは必ず読み直して構造が壊れていないか確認してからコミットメッセージ案を提案する。

### SNS投稿の判断ルール（私が自動で判断する）

**投稿する**：新機能・画面追加 / ゲーム動作が変わるバグ修正 / ビジュアル変化

**投稿しない**（`[skip-sns]` を付ける）：ドキュメント更新 / 設定変更 / リファクタリング / インフラ整備 / 軽微な仕様修正

投稿する場合はコミットメッセージがそのまま X・Bluesky への投稿文になるため、SNS映えする文体（日本語・絵文字なし・ハッシュタグはスクリプトが付ける）で書く。

## 定期整理フロー

演出・機能のひと区切りごと（バックログの演出エフェクト欄が空になったタイミングなど）に実施する：

1. `/simplify` — 重複・複雑化を圧縮する
2. `/code-review` — 整理後にバグ・問題が残っていないか確認する（**自動実行しない。推奨タイミングを提案してユーザーが判断する**）

### `/code-review` 推奨タイミング（私が提案する基準）

以下が重なったときに「`/code-review` を実行しますか？」と提案する：

- `await` / Signal / タイマー連鎖を含む変更が入ったコミットが2件以上積まれた
- 戦闘ロジック（ダメージ計算・フェーズ制御・勝敗判定）に触れた
- `/simplify` を実行した直後

**リネーム・@export追加・const抽出・ドキュメント更新のみのコミットは対象外。**

## このファイルの更新ルール

**設計方針が変わったら、その場でこのファイルも更新する。コミットより先に。**

- 技術選定・UI方針・画面設計が変わったら該当箇所を書き換える
- 「後で直す」はしない。古い情報が残るとセッション切れ後に誤実装の原因になる
- 仕様書（docs/）の内容はここに転記しない。参照ルールだけ書く
