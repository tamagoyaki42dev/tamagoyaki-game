# バックログ（未実装タスク）

セッション開始時に自動表示される。完了したら該当行を削除すること。

---

## 演出化粧（演出土台完成後）

**プロト1でやる（2026-07-04 決定・軽微な数値調整のみ）**
- [x] ~~PlayerGrid の位置をエディタ上でずらしてキャラを右上寄りに配置~~ **完了（2026-07-04）**。`battle.tscn` の PlayerGrid（EnemyGrid含む）をギズモで直接移動・目視確認済み
- [ ] 戦闘テンポを「読む瞬間」だけ遅くして可読性を上げる（一律スローは反復プレイで冗長化するので不可）。`battle_scene.gd` の `unit_action_show_duration` 0.6→0.9 / `rotate_show_duration` 0.35→0.6 を変更して目視。物足りなければ追加調整

**リリース後の一挙改善パスに回す（2026-07-04 決定・新規作り込みのため）**
- [ ] グロー/ブルーム（WorldEnvironment glow_enabled）
- [ ] 勝利・敗北演出をもう少し長く・派手にする（現状あっさりしすぎ）
- [ ] **戦闘速度トグル（1x / 2x）** を追加する。`Engine.time_scale` 標準機能で実装可。見やすさ（初見）と反復の快適さ（コア層）は速度固定では両立しないため、コア体験に効く。`@export` で倍率を持たせ UI から切替

## アート

- [x] ~~作風層スパイク（マット粘土）~~ **完了（2026-06-30・white採用）**。clay shader（段階ベタ塗り＋暖色影＋`smoothstep`境界）＋フレネルリム（中立白）＋スクリーン輪郭線（輝度エッジ）を味方・敵キャラに適用。`clay_*` の `@export` 調整盤あり。背景は載せない判断。詳細は devlog/2026-06-30

### 既視感をさらに削る施策・武器周りの見た目調整

→ **`docs/proto2_design.md` へ移行済み（2026-07-04）**。プロト1では対応せず、リリース後のグラフィック一挙改善パスでまとめて着手する方針に決定（①〜⑤・顔差し替え・武器の振り方修正・握り位置微調整を含む）


## サウンド

- [x] ~~SE/音声の実装~~ **完了（2026-06-29）**。AudioManager オートロード＋フリー素材で、攻撃ヒット（職別 slash/punch/magic/arrow 出し分け）・クリティカル・死亡・自己回復/列回復・ローテ選択/ステップ・補助・石化/石化解除・UI操作音まで接続済み
- [ ] （任意）`SE_ENEMY_HIT` / `SE_ENEMY_ROW` / `SE_SHIELD` は定数定義済みだが未接続。敵被弾音・シールド音を鳴らすなら接続する
- [x] ~~BGM ファイル名を中身に合わせてリネームする~~ **完了（2026-07-04）**。`bgm_menu.ogg`→`bgm_battle3_dragon.ogg`（実はドラゴン戦曲）／`bgm_battle3.mp3`→`bgm_menu.mp3`（実はメニュー曲）に実ファイル・`.import`・`audio_manager.gd`の定数を一致させてリネーム。GUT全175テストPASS

## UI（その他）

- [x] ~~タイトル画面＋マスター音量調整~~ **実装完了（2026-07-03）・目視確認済み（2026-07-04）**。`scenes/title_screen.tscn`／`AudioManager.set_master_volume`（`user://settings.cfg`永続化）
- [x] ~~ゲームタイトル決定~~ **「Perrant（ペラント）」に確定（2026-07-04）**。Perennial（永劫・繰り返す）＋errant（さすらう・遍歴の）の合成語。title_screen.gd（英字ロゴ＋上部にルビ表示）・project.godot の config/name（旧「ローテーションハクスラ」）ともに反映済み
- [x] ~~編成画面に「前に回す/後ろに回す」ボタンを追加~~ **実装完了（2026-07-04）・目視確認済み（2026-07-04）**。`GameState.rotate_formation(forward)`（列固定・行だけ戦闘と同じ周期で回転）＋`formation_ui.gd`にボタン2個をグリッド右側（前列側=上／後列側=下）に配置。押した際にカードがスライド移動するTween演出も追加（`rotate_slide_duration`で調整可）。GUT4件PASS
- [x] ~~敵の特記事項（notes）を実装~~ **完了（2026-07-04）**。`enemy_info_ui.gd`の特記事項欄に3体分のフレーバーテキストを追加。3体を貫く「澱み」の世界観（滅び→再興のループで滅んだ文明が澱み、濃度が上がるほど怪物が強く聡くなる）を新設し矛盾なく統一。実装解につながる情報（B2のローテ連動回復・B3の防御補助集中等）はステータス面から匂わせない方針で除外。折り返し表示バグ（`custom_minimum_size`をtext設定前に入れる必要があった）と全角ダッシュのフォント欠けを修正、`AUTOWRAP_WORD_SMART`＋文単位の手動改行で読みやすく調整。目視確認済み
- [x] ~~タイトル画面にDEBUGボタンを追加~~ **完了（2026-07-04）**。title_screen.tscnがmain_sceneになったことで`debug_launcher.tscn`への導線が無くなっていたため、左下に控えめな半透明「DEBUG」ボタンを追加。目視確認済み
- [ ] **【リグレッション】戦闘画面：3D戦場の味方キャラにカーソルを置いた時、右パネルの同キャラ行がハイライトされる機能が効いていない**（2026-07-04報告）
  - 対象は編成画面でなく**戦闘画面**（`battle_scene.gd` / `battle_ui.gd`）と判明済み。双方向の仕組み自体はコード上に現存：
    - 3D→パネル：`battle_scene.gd:892` `_spawn_hover_area`（プレイヤーキャラにArea3D付与）→`_on_unit_area_entered/exited`（906-910）→`unit_3d_hovered`シグナル発火→`battle_ui.gd:367` `_on_unit_3d_hovered`が行の背景色を変更
    - パネル→3D：`battle_ui.gd:228` 行の`mouse_entered/exited`→`_on_entry_hover`(360)→`unit_row_hovered`シグナル→`battle_scene.gd:913` `_on_row_hovered`が足元リングを発光
  - 静的に読む限り配線は繋がって見える＝**まず実機で「どちら向きが効いていないか」を確認するのが先決**（3D→パネルのみ死んでいる場合、同日直った編成画面の`mouse_filter=STOP`吸収バグと同型＝battle_ui側のフルスクリーンControlが3DのArea3Dマウスピッキングをブロックしている可能性が濃厚）
  - GUT対象外（マウスホバー・マテリアル色は視覚のみ）。修正後は実機目視確認が必須

## ロジック設計・仕様確定

- [x] ~~【バグ】編成が7人上限を超えて配置できる~~ **修正完了（2026-07-04）**。`GameState.place()`に人数上限チェックが無く、12マスのグリッドに8人以上配置できてしまっていた。`MAX_PARTY := 7`定数を追加し、グリッド外のキャラを空きマスに置いて8人目になる場合のみ拒否（グリッド内の再配置・スワップは人数不変のため妨げない）。拒否時は現状無反応（フィードバック無し）で確定。回帰テスト3本追加（7人まで許可／8人目拒否／満員での再配置は妨げない）。GUT全178テストPASS
- [x] ~~【最優先バグ】敵の自己回復がSTAY時も毎ターン発動している~~ **修正完了（2026-07-04）**。`battle_manager.gd:84` を `do_rotate` gate に修正。B2はSTAY戦略で一切ローテしないため修正後はregen42がSTAY中は永久に無風になる構造判明＝「壁」の主表現をHPへ完全移行しHP150→350へ再格上げ（regen42はRotateチート潰し用として維持）。B2-S 100%@6T・B2-R 0%@11T・B2-W 100%@5Tで修正前と同水準に収束。B3はADAPTIVE戦略（4ターン中3回ローテ）のためregen6は実質無傷・数値変更なし。GUT全171テストPASS・独立検算済み。詳細 devlog/2026-07-04
- **味方15職リバランス完了（2026-07-01）→ 敵側の再較正 完了（2026-07-02）**
  - [x] ~~味方15職フルリバランス~~ **完了（2026-07-01）**。レンジ81〜91・dominance なし。character_data.gd / test CHAR_BASE / unit_spec.md 反映済み。チェッカー式も修正（連撃×0.8→×1.5・会心×N・自回復0.8→1.0）。詳細 devlog/2026-07-01
  - [x] ~~敵スタットの再調整＋シム再較正~~ **完了（2026-07-02〜07-03）**。B1は列攻撃を薄く追加。B3は「力を溜める→列攻撃」がAoE×「1体でも倒れたら即撤退」仕様と衝突し詰みだったため「力を溜める→通常(2倍)」に変更＋HP225/ATK11に再調整。SMART戦略導入で正しく較正。B2は過程で `ThoughtType.STRONG_TARGET` の表示文言（攻撃力基準）と実装（現在HP基準）の食い違いバグを発見＋`HIGH_HP_TARGET`/`HIGH_ATK_TARGET`に分離（詳細下記）。最終値：B2 HP150/ATK8/regen42/HIGH_HP_TARGET。`ASSERT_BALANCE=true` 維持、全171テストPASS。詳細 devlog/2026-07-02, devlog/2026-07-03
  - [x] ~~`ThoughtType.STRONG_TARGET` バグ修正＋思考タイプ分離~~ **完了（2026-07-03）**。「強者狙い」の表示文言は攻撃力基準なのに実装は現在HP基準というバグを発見。`HIGH_HP_TARGET`（旧実装を継承・現在HP基準）と`HIGH_ATK_TARGET`（新規・攻撃力基準）に分離した（enemy_data.gd / battle_manager.gd / enemy_generator.gd THOUGHT_WEIGHTS / enemy_spec.md 反映済み）。**発見**：`HIGH_ATK_TARGET`（常に同じ最強火力を専任で狙う）は、防御補助/シールドがローテでしか復活せずSTAY中は1回のみ有効なため、「STAYで押し切る」設計のB2とは構造的に衝突する（固定標的が長期戦で必ず力尽きる）。B2は`HIGH_HP_TARGET`のまま据え置き、`HIGH_ATK_TARGET`はプロト2以降の生成敵で活かす
  - [x] ~~職ごとのステータス上限・下限レンジを数値で定義する（unit_spec.md 未決定事項）~~ **完了（2026-07-04）**。unit_spec.mdに「ステータス上限・下限（設計ガードレール）」の節を新設。8ステータス（HP/ATK/SPD/間接ATK/攻補/防補/自回復/列回復）ごとに壊れる具体シナリオから逆算した上限・下限を定義。独立検算済み（当初案の2箇所の根拠文言の数値矛盾を修正）

- **3戦バランス検証**（2026-06-30 割り直し。旧「完了」マークは早すぎた＝ツールのビルド完了をゴール達成に横滑りさせていた。CLAUDE.md タスク管理「道具を作る／道具で確かめるは別タスク」参照）
  - [x] ~~シムツール本体 ＋ 全7シナリオ（B1-R/S・B2-S/R・B3-A/R/S）Pass~~ **完了（2026-06-30）**。詳細は devlog/2026-06-30
  - [x] ~~**DIV＝各戦を構成違いの2〜3編成で回し、単一ビルド固定でないことを確認**~~ **完了（2026-06-30）**。発見：3戦は degree でなく kind でランプ（B1=消耗死／B2=DPS壁／B3=スパイク死）。詳細 devlog/2026-06-30
  - [x] ~~**【道具】シムに汎用 competent-player 戦略（SMART）を実装**~~ **完了（2026-07-02）**。1手先読み貪欲プレイヤー（石化倍率・防御補助・思考タイプの狙い先読みまで考慮）。B3-A(良編成)100%・B3-W(良編成)31〜37%・悪編成8〜16%と正しく弁別。旧固定戦略（ROTATE/STAY/ADAPTIVE_B3）は危険ターンに脆いユニットを前へ晒し原理的に測れなかった問題を解消。[[proto2以降]] の「生成敵を自動ビート判定するEV採点器」の前身として残す
  - [ ] **手動プレイテストで体感勝率を確認（ユーザーの仕事）**→「もっと強く／弱く」を指示。現行値（B1-R86%@9.9T・B2-S100%@6T・B3-A100%@6T）を基準に体感とズレがないか確認。`proto1_3battle_design.md` §5.6 で手動送りと明記済み
  - [x] ~~ランプ基準ノートの再検証（2026-07-04）~~ **完了**。2026-07-02時点の「B1(消耗戦)12.4T > B2(DPS壁)11.0T > B3(コンボ)6〜8T」は敵自己回復バグ修正前の数字のままで陳腐化していたと判明（B2は修正後6Tに短縮）。シム再実測の結果 **B1(消耗戦)9.9T が突出して長く、B2(DPS壁)6.0T・B3(コンボ)6.0〜7.0Tはほぼ同着**という像に修正。「強い」は数字膨張でなく①戦略要求が鋭くなる②正解戦略での勝ち幅が戦ごとに細る、で表す方針は変更なし
  - [x] ~~B1（マッシュナブ）のターン数調整~~ **完了（2026-07-04）**。体感で「B1だけ長すぎる」との指摘を受け、目標9〜10Tから逆算。HP360→280（比例計算）→勝率94%に上振れたためATK13→14で相殺（16は効きすぎて46%まで急落・高感度ゾーンと判明）。最終値 HP280/ATK14 で勝率86%@9.9T。GUT全175テストPASS

- [x] ~~**「攻補(列)」を全レイヤーから除去**~~ **完了（2026-06-30）**。コード（battle_manager/character_data/battle_unit）・UI（formation_ui/battle_ui）・テスト・ドキュメント6ファイルを一括除去。全170テスト PASS。`await`/Signal を含むため**実装後に1戦を通して攻撃補助演出を目視確認**が必要
- [x] ~~**仕様書の `initial_attack_mult`→`initial_attack_bonus` 取り残しを修正**~~ **完了（2026-06-30）**。`enemy_spec.md`・`battle_spec.md` を加算 int（B1=0/B2=4/B3=5）の実態に合わせて更新


## プロト2以降の検討

→ **`docs/proto2_design.md` へ移行済み（2026-07-02）**。個体値＋偏差／手続き生成2原則／敵行動乱数／必殺ボタン／KayKit基盤の5件はそちらを参照。プロト2着手時にそこから backlog へタスクを切り出す
